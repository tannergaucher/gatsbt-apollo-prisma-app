import styled from 'styled-components'

const WithBadge = styled.div`
  margin-bottom: 4em;
  position: relative;
  .absolute {
    position: absolute;
    bottom: -2%;
    right: -2%;
  }
`

export default WithBadge
