// function getUserGoing(userEvents, allEvents) {
//   const eventIds = []
//   userEvents.map(userEvent => eventIds.push(userEvent.eventId))

//   const isUserEventNode = []
//   allEvents.map(edge => {
//     const { id } = edge.node.frontmatter
//     if (eventIds.indexOf(id) !== -1) {
//       isUserEventNode.push(edge)
//     }
//   })
// }

// export default getUserGoing
